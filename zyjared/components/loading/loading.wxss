._loading-wrapper {
    margin: auto;
    width: fit-content;
}

._loading {
    animation: loading linear infinite 1s;
}

@keyframes loading {
    from {
        transform: rotate(0);
    }

    to {
        transform: rotate(360deg);
    }
}